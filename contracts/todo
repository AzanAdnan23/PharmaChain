// all checks and error handling pending

//for example check is user already registered or not

// bathces ka struct ma isquality disaprove dalna ha

// how to implement rfid logic from distributor to providers when batches are broken down into units.
